# Legacy OCR/Translation Job Handlers

Frozen reference copies of standalone OCR/translation job handlers.

Purpose:

- preserve pre-rewrite behavior for reference
- keep runtime detached while workflow rewrite is in progress

Files:

- `translation_job_handler_legacy.py`

Status:

- read-only reference
- not used by active runtime routing
